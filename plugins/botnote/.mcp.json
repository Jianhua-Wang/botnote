{
  "mcpServers": {
    "botnote": {
      "command": "bash",
      "args": [
        "-lc",
        "for d in \"${CLAUDE_PLUGIN_ROOT:-}\" \"${CODEX_PLUGIN_ROOT:-}\" \"${CURSOR_PLUGIN_ROOT:-}\" \"$PWD\"; do if [ -n \"$d\" ] && [ -x \"$d/scripts/run-mcp.sh\" ]; then exec \"$d/scripts/run-mcp.sh\"; fi; done; echo 'botnote run-mcp.sh not found' >&2; exit 127"
      ],
      "cwd": ".",
      "env": {
        "BOTNOTE_URL": "${user_config.botnote_url}",
        "BOTNOTE_TOKEN": "${user_config.botnote_token}"
      }
    }
  }
}
